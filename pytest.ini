[pytest]
testpaths = tests
pythonpath = .
addopts = -v --cov=app --cov=model --cov-report=term-missing
filterwarnings =
    # joblib 1.5.3 has not caught up with the NumPy 2.5 deprecation.
    ignore:Setting the shape on a NumPy array has been deprecated:DeprecationWarning
